<form method="post" action="{{ path('app_weather_conditions_delete', {'id': weather_condition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ weather_condition.id) }}">
    <button class="btn">Delete</button>
</form>
